#include <stdio.h>
#define L 20

int un_a_un(char *s){
  int i=0;
  
  while(s[i]!='\0') i++;
  return i;
}

int main(){
 char s[L];
 
 fgest(s,L-1,stdin);
 printf("%d\n",un_a_un(s));
 
 return 0;
  
}